import { CardLoader } from './styled'

import loader from '../../assets/icons/loader.gif'

const Loader = (): JSX.Element => {
  return <CardLoader src={loader} alt={'loader'} />
}

export default Loader
